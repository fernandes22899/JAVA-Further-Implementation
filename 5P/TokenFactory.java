import java.util.*;

/**
 * TokenFactory: a factory class, which makes tokens from String fields.
 * This is an example of a Factory pattern.
 *
 * @author rdb
 *
 */
public class TokenFactory
{
    /**
     * Create the Lexer.
     * If there is a command line argument use it as an input file.
     * otherwise invoke an interactive dialog.
     *
     * @param s String input for creating token.
     *
     * @return New token object.
     */
    public static EToken makeToken( String s )
    {
        
        return null;
    }

}
